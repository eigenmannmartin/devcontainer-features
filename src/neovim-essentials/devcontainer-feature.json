{
  "name": "NeovimEssentials",
  "id": "neovim-essentials",
  "version": "0.1.5",
  "description": "Provide essentials features for neovim for devcontainers",
  "options": {
    "git": {
      "type": "boolean",
      "description": "If true, git will be installed",
      "default": true
    },
    "curl": {
      "type": "boolean",
      "description": "If true, curl will be installed",
      "default": true
    },
    "fzf": {
      "type": "boolean",
      "description": "If true, fzf will be installed",
      "default": true
    },
    "bat": {
      "type": "boolean",
      "description": "If true, bat will be installed",
      "default": true
    },
    "ripgrep": {
      "type": "boolean",
      "description": "If true, ripgrep will be installed",
      "default": true
    }
  }
}
